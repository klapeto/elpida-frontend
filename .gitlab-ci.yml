image: node:12.18.1

pages:
  cache:
    paths:
      - node_modules/

  stage: deploy
  script:
    - npm install
    - npm run build --prod
    - mkdir public
    - mv dist/* public/
  artifacts:
    paths:
      - public
  only:
    - develop
