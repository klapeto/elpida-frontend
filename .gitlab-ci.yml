image: node:8.12.0

pages:
  cache:
    paths:
      - node_modules/

  stage: deploy
  script:
    - npm install
    - npm run build --prod
    - mv dist/* public/
  artifacts:
    paths:
      - public
  only:
    - master
    - pages
